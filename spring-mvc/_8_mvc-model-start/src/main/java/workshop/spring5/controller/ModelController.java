package workshop.spring5.controller;

import org.springframework.stereotype.Controller;

@Controller
public class ModelController {

    /*
        TODO 2
        Endpoint GET z dowolnym mapowaniem - takim, które nie koliduje z istniejącym(i).

        Metoda zwraca String (nazwa widoki), przyjmuje parametr Model.
        W środku metody tworzona jest Mapa, wktóryej kluczem i wartością jest String.
        Do mapy dodajemy 'first' i 'second' z dowolnymi wartościami.

        Dodaj (bezpośredniu do obiektu model) 'third'/

        Użyj metody mergeAttributes,żeby dodać mapę atrybutów do modelu.

     */
}
