package workshop.spring5.controller;

import org.springframework.stereotype.Controller;

@Controller
public class ModelMapController {

    /*
        TODO 3
        Endpoint GET z dowolnym mapowaniem - takim, które nie koliduje z istniejącym(i).

        Metoda zwraca String (nazwa widoku) i jako parametr przyjmuje ModelMap
        W implementacji dodajemy do ModelMap 'first' i 'second' z dowolnymi warościami.

     */
}
